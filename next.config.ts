import type { NextConfig } from "next";

// Default Next config; enable standalone output here if you need a slimmer container layout later.
const nextConfig: NextConfig = {};

export default nextConfig;
